<Window x:Class="RevitProjectDataAddin.柱配筋施工図"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="柱配筋施工図"
        Width="1150"
        Height="Auto"
        FontFamily="Yu Mincho"
        Background="White"
         ResizeMode="NoResize">

    <!-- Định nghĩa màu sắc -->
    <Window.Resources>
        <SolidColorBrush x:Key="TitleColor1" Color="Blue"/>
        <SolidColorBrush x:Key="TitleColor2" Color="Orange"/>
        <SolidColorBrush x:Key="Label"      Color="Green"/>
        <SolidColorBrush x:Key="注意"       Color="Red"/>
    </Window.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="10">
                <Grid Margin="10">

                    <!-- Hai cột bằng nhau -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <!-- 5 hàng auto -->
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Tiêu đề chính -->
                    <TextBlock Text="柱配置"
                   FontSize="30"
                   Grid.ColumnSpan="2"
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
                    <TextBlock Foreground="{StaticResource TitleColor1}" Text="通りを選択" FontSize="20" Margin="0,50,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>

                    <StackPanel Orientation="Vertical" Margin="0,0,0,0"  Grid.Row="1">
                        <!-- Lựa chọn 階 -->
                        <TextBlock Text="X通を選択"
                           Grid.Column="0"
                           FontSize="18"
                           VerticalAlignment="Center"/>
                        <ComboBox  x:Name="X通を選択ComboBox"
                          Grid.Column="0"
                          Margin="0,0,20,0"
                          FontSize="18"
                          Height="32"                    
                             SelectedItem="{Binding X通を選択, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  />
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Margin="0,0,0,0" Grid.Row="1"  Grid.Column="1">
                        <!-- Lựa chọn 通 -->
                        <TextBlock Text="Y通を選択"
                           Grid.Column="1"
                           FontSize="18"
                           VerticalAlignment="Center"/>
                        <ComboBox  x:Name="Y通を選択ComboBox"
                          Grid.Column="1"
                          Margin="0,0,20,0"
                          FontSize="18"
                          Height="32"   
                                   
                             SelectedItem="{Binding Y通を選択, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  />
                    </StackPanel>

                    <!-- Hướng dẫn nhập liệu -->
                    <TextBlock Text="プレビュー"
                   Grid.Row="2"
                   Grid.ColumnSpan="2"
                   FontSize="20"
                   Foreground="{StaticResource TitleColor1}"
                   Margin="0,20,0,10"/>

                    <!-- Container để đổ các StackPanel động -->
                    <Border Grid.Row="3" Grid.ColumnSpan="2"
                BorderBrush="Silver"
                BorderThickness="1"
                Padding="10"
                Background="White">
                        <StackPanel x:Name="StackPanelContainer"
                      Orientation="Vertical"
                      Margin="0">
                            <ItemsControl ItemsSource="{Binding BeamSegments}" Margin="10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="10">
                                            
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
